define([
    'underscore', 
    'backbone', 
    'models/cow'
], function(_, Backbone, Store, Cow){

    var KineCollection = Backbone.Collection.extend({

	model: Cow,

    });

    return KineCollection;
});
